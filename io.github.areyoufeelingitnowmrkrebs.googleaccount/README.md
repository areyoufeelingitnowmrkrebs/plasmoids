# Google Account Singleton
This is not a widget, but rather a dependency for the *actual* Google widgets; it allows you to just add one and then sign into it before adding more, which will sign you into the others automatically before you even add them, rather than making you sign into each one individually.

Must go in `~/.local/share/plasma/plasmoids` alongside the actual widgets.
